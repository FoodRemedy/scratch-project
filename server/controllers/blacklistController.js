const User = require('../models/userModel');
const blacklistController = {};

  // IT *MUST* RECIEVE AN ARRAY AS THE VALUE OF FAVORIES
  blacklistController.addBlacklist = async (req, res, next) => {};

  // IT *MUST* RECIEVE AN ARRAY AS THE VALUE OF FAVORIES
  blacklistController.deleteBlacklist = async (req, res, next) => {};

  blacklistController.getBlacklist = async (req, res, next) => {}

module.exports = blacklistController;